export class WindowController {
    // Method to get details of a window
    getWindowDetails(req, res) {
        // Logic to retrieve window details
        res.send("Window details");
    }

    // Method to update window information
    updateWindow(req, res) {
        // Logic to update window information
        res.send("Window updated");
    }
}